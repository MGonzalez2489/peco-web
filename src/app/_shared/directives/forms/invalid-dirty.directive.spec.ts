import { InvalidDirtyDirective } from './invalid-dirty.directive';

describe('InvalidDirtyDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidDirtyDirective();
    expect(directive).toBeTruthy();
  });
});
