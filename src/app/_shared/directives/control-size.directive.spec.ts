import { ControlSizeDirective } from './control-size.directive';

describe('ControlSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlSizeDirective();
    expect(directive).toBeTruthy();
  });
});
